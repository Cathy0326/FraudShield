{
  "title": "FraudShield Overview",
  "uid": "fraudshield-overview",
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "5s",
  "time": { "from": "now-15m", "to": "now" },
  "panels": [
    {
      "id": 1,
      "title": "Request rate (req/s)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "sum(rate(http_server_requests_seconds_count{application=\"fraudshield\"}[1m])) by (uri, status)",
          "legendFormat": "{{uri}} [{{status}}]"
        }
      ]
    },
    {
      "id": 2,
      "title": "p95 request latency",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_server_requests_seconds_bucket{application=\"fraudshield\"}[5m])) by (le, uri))",
          "legendFormat": "{{uri}}"
        }
      ]
    },
    {
      "id": 3,
      "title": "Risk events by level (rate)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "targets": [
        {
          "expr": "sum(rate(fraudshield_risk_events_total[1m])) by (riskLevel)",
          "legendFormat": "{{riskLevel}}"
        }
      ]
    },
    {
      "id": 4,
      "title": "Rule-evaluation duration (p95)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(fraudshield_risk_evaluation_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95"
        }
      ]
    },
    {
      "id": 5,
      "title": "JVM heap used",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "targets": [
        {
          "expr": "jvm_memory_used_bytes{application=\"fraudshield\", area=\"heap\"}",
          "legendFormat": "{{id}}"
        }
      ]
    },
    {
      "id": 6,
      "title": "Kafka consumer lag",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "targets": [
        {
          "expr": "sum(kafka_consumer_fetch_manager_records_lag{application=\"fraudshield\"}) by (topic)",
          "legendFormat": "{{topic}}"
        }
      ]
    },
    {
      "id": 7,
      "title": "Historic order mix by risk level (cumulative DB counts)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 24 },
      "description": "From DashboardMetricsExporter — republishes the same DB-backed dashboard stats RiskEventService already aggregates, as a time series so trends become visible (not just the current snapshot).",
      "targets": [
        {
          "expr": "fraudshield_dashboard_risk_level_count",
          "legendFormat": "{{riskLevel}}"
        }
      ]
    },
    {
      "id": 8,
      "title": "Risk rate over time (%)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 24 },
      "targets": [
        {
          "expr": "fraudshield_dashboard_risk_rate_percent",
          "legendFormat": "risk rate %"
        }
      ]
    },
    {
      "id": 9,
      "title": "Top triggered rules (historic hit counts)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 32 },
      "description": "Which detection rules are firing most over time — useful for spotting a rule that's gone noisy (too many false positives) or a fraud pattern that's shifting to evade a specific rule.",
      "targets": [
        {
          "expr": "fraudshield_dashboard_rule_hit_count",
          "legendFormat": "{{rule}}"
        }
      ]
    }
  ]
}
