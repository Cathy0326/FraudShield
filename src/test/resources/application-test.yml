# Integration test profile — disables Kafka/Redis so *IT.java tests use H2 only
spring:
  kafka:
    bootstrap-servers: localhost:9092
    admin:
      fail-fast: false
  data:
    redis:
      host: localhost
      port: 6379
      connect-timeout: 500

ai:
  enabled: false
