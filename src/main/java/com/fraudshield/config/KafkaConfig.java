package com.fraudshield.config;

import org.springframework.context.annotation.Configuration;

// Kafka producer/consumer beans will be added here in later prompts.
// NewTopic declarations are intentionally omitted from the skeleton:
// Kafka auto-creates topics on first produce (AUTO_CREATE_TOPICS_ENABLE=true),
// so there's no need to connect KafkaAdmin at startup before the broker is up.
@Configuration
public class KafkaConfig {
}
