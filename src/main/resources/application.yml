# ── Server ──────────────────────────────────────────────────────────────────
server:
  port: 8080

spring:
  # ── H2 In-Memory Database ─────────────────────────────────────────────────
  # Swap the url/driver/dialect lines for PostgreSQL when ready.
  datasource:
    url: jdbc:h2:mem:fraudshield;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  # H2 web console — useful during development (disable in prod)
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true   # allow access from non-localhost (Docker/remote)

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update   # auto-creates tables from @Entity classes
    show-sql: false       # flip to true for query logging

  # ── Redis ──────────────────────────────────────────────────────────────────
  # Used for sliding-window counters and short-lived risk state.
  data:
    redis:
      host: localhost
      port: 6379
      password: ""   # override via SPRING_DATA_REDIS_PASSWORD env var
      ssl:
        enabled: false   # Azure Cache for Redis requires SSL — override via SPRING_DATA_REDIS_SSL_ENABLED
      connect-timeout: 2000   # ms — don't block startup too long

  # ── Kafka ──────────────────────────────────────────────────────────────────
  # fail-fast=false lets the app start even when the broker is not yet reachable
  # Local dev / docker-compose: plaintext localhost:9092 (defaults below).
  # Production: point at an Azure Event Hubs Kafka-compatible endpoint via env vars —
  # see KAFKA_* below. Event Hubs requires SASL_SSL with the connection string as password.
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    admin:
      fail-fast: false
    # security.protocol has a dedicated KafkaProperties field — bind it there directly.
    # Putting it under the generic `properties:` map (like sasl.mechanism/sasl.jaas.config
    # below) doesn't reliably pick up env var overrides, because Map<String,String> relaxed
    # binding doesn't match dotted keys like "security.protocol" the way dedicated fields do.
    security:
      protocol: ${KAFKA_SECURITY_PROTOCOL:PLAINTEXT}
    properties:
      sasl.mechanism: ${KAFKA_SASL_MECHANISM:PLAIN}
      sasl.jaas.config: ${KAFKA_SASL_JAAS_CONFIG:}
    # Producer defaults
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    # Consumer defaults
    consumer:
      group-id: fraudshield-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "com.fraudshield.*"

# ── JWT ──────────────────────────────────────────────────────────────────────
# secret必须保密；这里是base64编码的字符串，对应 "fraudshield-secret-key-2026-hackathon"
# Keep the secret out of VCS in production — use env var or vault instead
jwt:
  secret: "ZnJhdWRzaGllbGQtc2VjcmV0LWtleS0yMDI2LWhhY2thdGhvbg=="
  expiration-ms: 86400000   # 24 hours in milliseconds

# ── CORS ───────────────────────────────────────────────────────────────────
# Comma-separated list of allowed browser origins. Override via CORS_ALLOWED_ORIGINS
# env var in production (the frontend itself talks same-origin through nginx's reverse
# proxy, so this mainly matters for direct API access during local dev / tooling).
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# ── App-level custom properties ───────────────────────────────────────────────
fraudshield:
  kafka:
    topic:
      order-events: order-events   # topic name used by producers/consumers
  encryption:
    # PII落库加密密钥：32字节的Base64（生成：openssl rand -base64 32）
    # 未设置时PII明文存储并打启动警告 —— 仅限本地开发
    # PII-at-rest key: 32 bytes, base64 (generate: openssl rand -base64 32).
    # Unset = plaintext storage with a startup warning — local dev only.
    key: ${FRAUDSHIELD_ENCRYPTION_KEY:}

# ── Azure OpenAI ──────────────────────────────────────────────────────────────
# 凭证通过环境变量或.env文件注入（不提交到git）
# Credentials are injected via env vars / .env file — never committed to VCS
azure:
  openai:
    endpoint: ${AZURE_OPENAI_ENDPOINT:}
    key: ${AZURE_OPENAI_KEY:}
    deployment: ${AZURE_OPENAI_DEPLOYMENT:gpt-4.1-mini}

# ai.enabled=false 时跳过所有LLM调用（本地开发无需配置密钥）
# Set ai.enabled=false to skip all LLM calls (useful for local dev without credentials)
ai:
  enabled: true

# ── Observability ──────────────────────────────────────────────────────────
# Only expose health/info/prometheus — never the full endpoint set (env, beans,
# heapdump etc. leak internals and are a real attack surface if left open).
# Scraping is unauthenticated here; in a real deployment this is locked down at
# the network layer (Container Apps internal ingress / k8s NetworkPolicy), not
# behind app-level auth, since Prometheus itself doesn't send a Bearer token.
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never
  metrics:
    tags:
      application: fraudshield
